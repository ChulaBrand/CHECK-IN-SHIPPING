# Copia este archivo a .env.local para probar en tu máquina.
# .env.local nunca se sube a git (ver .gitignore).

# URL del Google Apps Script publicado como Web App (termina en /exec).
# Ver README para los pasos de cómo crearla en tu Google Sheet.
NEXT_PUBLIC_APPS_SCRIPT_URL="https://script.google.com/macros/s/TU_ID_AQUI/exec"
